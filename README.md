# Набор утилит для Windows (BAT/CMD)  Сборник скриптов для автоматизации задач в Windows.

## Содержание
### 📁 0x81000203
| Файл | Описание |
|------|----------|
| `0x81000203.bat` | Восстанавливает службу теневого копирования (ошибка 0x81000203). |
### 📁 Devcon
| Файл | Описание |
|------|----------|
| `Install_Devcon.bat` | Устанавливает утилиты `devcon.exe` / `devcon64.exe` в систему (должны лежать рядом). |
### 📁 Enable-Windows-Defender
| Файл | Описание |
|------|----------|
| `Enable_Windows_Defender.bat` | Включает Защитник Windows и центр безопасности через импорт `WinDefend.reg` и `wscsvc.reg`. |
### 📁 Firewall
| Файл | Описание |
|------|----------|
| `Block_EXE.bat` | Добавляет правила в брандмауэр Windows для блокировки исходящих соединений всех `.exe` в текущей папке и подпапках. |
| `Firewall_OFF.bat` | Полностью отключает брандмауэр Windows для всех профилей. |
| `Firewall_ON.bat` | Включает брандмауэр Windows. |
#### 🎮 Games
| Файл | Описание |
|------|----------|
| `HUNT_SHOWDOWN_1896_PORTS.bat` | Интерактивное меню для управления правилами брандмауэра Hunt: Showdown 1896. Порты: TCP/UDP 61088, 20000-20099, 3074, 3478, 4379-4380, 27000-27050. |
| `DBD_PORTS.bat` | Управление правилами для Dead by Daylight. Порты: TCP/UDP 4379, 4380, 3478, 27015-27030, 27036-27037, 27000-27037. |
| `EGS_PORTS.bat` | Управление правилами для Epic Games Store (только основные порты). Порты: TCP 80, 443, 3478, 3479, 5060, 5062, 5222, 6250; UDP 3478, 3479, 5060, 5062, 5222, 6250. |
### 📁 NIRcmd
| Файл | Описание |
|------|----------|
| `NIR_Timer.bat` | Задержка на заданное количество минут, затем имитация нажатия `Ctrl+Tab` и пробел (требуется `nircmd.exe`). |
### 📁 System-Recovery
| Файл | Описание |
|------|----------|
| `CBSLog_To_TXT.bat` | Извлекает строки с `[SR]` из CBS.log и сохраняет на рабочем столе как `sfcdetails.txt`. |
| `ChkDsk.bat` | Запускает `chkdsk /f /r` (проверка диска при следующей загрузке). |
| `Restore_Health.bat` | Запускает `DISM /RestoreHealth` для восстановления образа системы. |
| `Scan_Health.bat` | Запускает `DISM /ScanHealth` – проверка образа системы на повреждения. |
| `Scan_Now.bat` | Запускает `sfc /scannow` – проверка целостности системных файлов. |
| `Store_Apps_Reset.cmd` | Сброс Microsoft Store и приложений Windows 10 (очистка кэша, перерегистрация). |
| `Repair_Services_Win_11.bat` | Восстанавливает автоматический запуск системных служб Windows 11. Устанавливает тип запуска `Auto` и запускает все критические службы (от CryptSvc до XboxNetApiSvc). Выводит цветной отчёт о статусе каждой службы. Требует права администратора. |
### 📁 py-scripts
| Файл | Описание |
|------|----------|
| `create_folders_and_empty_json_files.py` | Создаёт 25 папок с именами 001–025, в каждой из которых генерирует 25 пустых JSON-файлов вида 001_001.json. Полезно для тестирования файловых операций или создания структуры данных. |
| `matrix_size_benchmark.py` | Бенчмарк производительности матричного умножения на CUDA. Тестирует размеры от 256 до 4096 в FP32/TF32 и FP16 через PyTorch (и Triton при наличии). Вычисляет TFLOPS, ускорение FP16 и эффективность Tensor Cores (для RTX 3070). Помогает выбрать оптимальный размер матриц для максимальной производительности. |
| `dump_for_ai.py` | Создаёт Markdown-дампы всех текстовых файлов проекта, рекурсивно обходя папки (исключая `.git`, `__pycache__`, бинарные и слишком большие файлы). Полезно для передачи контекста в LLM или анализа кода. |
### Другое
| Файл | Описание |
|------|----------|
| `ADB_WiFi.bat` | Подключение к Android-устройству по ADB через Wi-Fi (выбор IP). |
| `AdminC_heck.bat` | Проверка, запущен ли скрипт от имени администратора. |
| `Create_Shortcut.bat` | Создаёт ярлык на рабочем столе для перетащенного `.exe`. |
| `Driver_Signature.bat` | Управление проверкой цифровой подписи драйверов и тестовым режимом (3 пункта, русский интерфейс). |
| `FTP.bat` | Открывает в проводнике FTP-серверы по локальным IP (порт 6559) с быстрой проверкой доступности через ping. |
| `Flush_DNS.bat` | Сбрасывает Winsock, IP и DNS-кэш. |
| `Game_DVR_Timer.bat` | Изменение максимальной длительности записи Xbox Game Bar (MaximumRecordLength) в HKCU\...\GameDVR. Поддерживает тайминги 5/10/15/30 мин, 1/2/4 часа. |
| `Icon_Cache_Cleaner.bat` | Очищает кэш иконок и перезагружает компьютер. |
| `Restart_TPLink.bat` | Перезапускает сетевой интерфейс "TP-Link" при потере связи с 8.8.8.8. |
| `Shutdown_Timer.bat` | Устанавливает таймер выключения компьютера (в минутах) с корректной отменой. |
| `Startup_Folders.bat` | Открывает папку автозагрузки текущего пользователя или всех пользователей. |
| `System_Score.bat` | Запускает оценку производительности Windows (WinSAT) или показывает разобранные результаты из XML. |
| `Unhide_Files.bat` | Снимает атрибуты `скрытый`, `системный` и `только чтение` со всех файлов в текущей папке. |

## ⚙️ Требования

- Windows 7 / 8 / 10 / 11 (некоторые функции могут быть недоступны в старых версиях).
- Для `ADB_WiFi.bat` требуется [ADB (Android Debug Bridge)](https://developer.android.com/studio/releases/platform-tools) в `PATH` или рядом.
- Для `NIR_Timer.bat` требуется [nircmd.exe](https://www.nirsoft.net/utils/nircmd.html) в той же папке или в `PATH`.
- Для `Install_Devcon.bat` нужны файлы `devcon.exe` и `devcon64.exe` из Windows Driver Kit (WDK) в той же папке.
- Для `Enable_Windows_Defender.bat` нужны файлы `WinDefend.reg` и `wscsvc.reg` (экспортированные ветки реестра с работающей системы).

## 🚀 Использование

1. Скачайте нужный `.bat` или `.cmd` файл.
2. **Запускайте от имени администратора** (кроме тех, где не требуется прав).
3. Следуйте инструкциям на экране.

Большинство скриптов после выполнения предлагают перезагрузить компьютер – это нормально для применения изменений.

## 📄 Лицензия

Данный набор скриптов распространяется как есть, без каких-либо гарантий. Вы можете свободно использовать, изменять и распространять их с указанием авторства.

## ⚠️ Внимание

Некоторые скрипты изменяют системные настройки (брандмауэр, подписи драйверов, реестр). Делайте резервные копии и используйте на свой страх и риск. Рекомендуется сначала протестировать в виртуальной среде.

---

📌 **Совет:** для удобства поместите все скрипты в одну папку и добавьте её в `PATH`, чтобы вызывать их из командной строки без указания полного пути.
